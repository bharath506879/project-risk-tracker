[build]
  publish = "frontend"
  command = "echo 'No build needed for static site'"

[[redirects]]
  from = "/*"
  to = "/riskguard.html"
  status = 200

[context.production.environment]
  NODE_ENV = "production"
